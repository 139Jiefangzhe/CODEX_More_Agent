# 项目进度记录（2026-03-29）

更新时间：2026-03-29 23:05:53 +08:00

## 1. 今日已完成

本轮目标是继续补全“控制链路 + 事件链路 + 文档对齐”，当前已落地：

- 修复 `redis` 可选依赖导致的构建失败（未安装 `redis` 也可正常 `build`）。
- 事件总线支持 `emitter/redis`，并在配置不完整时自动降级。
- `control_signal -> control_applied` 闭环增强，终态 run 的遗留控制信号会自动收敛消费。
- `dashboard + coder + implementing` 场景下，`pause/resume` 支持“软即时控制”（API 下发即改状态）。
- `abort` 路径防回归：避免会话/运行已 `aborted` 后又被异常流覆盖成 `failed`。
- 增强 codex 轮询稳定性（短超时轮询 + transient 错误继续轮询）。
- 增加 SSE 验证路径：`/api/stream` 可收到 `internal/notify` 注入事件。
- 文档已同步到“可运行状态”，不再描述为“纯骨架”。

## 2. 关键改动文件

- `src/dashboard/server/services/event-bus.ts`
- `src/dashboard/server/routes/agents.ts`
- `src/dashboard/server/services/agent-service.ts`
- `src/dashboard/server/services/orchestrator-service.ts`
- `src/dashboard/server/services/openai-responses-client.ts`
- `src/dashboard/server/services/codex-executor.ts`
- `src/dashboard/.env.example`
- `src/dashboard/README.md`
- `docs/architecture/project-structure-summary.md`

## 3. 验证结果

已完成验证：

- `npm run build` 通过（server + client）。
- Hook 脚本语法检查通过：
  - `.claude/scripts/dashboard/db-utils.sh`
  - `.claude/scripts/dashboard/event-emitter.sh`
  - `.claude/scripts/dashboard/checkpoint.sh`
- `internal notify` 验证通过：
  - `kind=control_applied` 经 SSE 收到消息。
  - `kind=agent_event` 经 SSE 收到消息。
- `pause/resume`（dashboard coder implementing）验证通过：
  - `pause` 返回 `mode=immediate` 且 `run_status=paused`
  - `resume` 返回 `mode=immediate` 且 `run_status=running`
  - 对应 `control_signals` 在库中 `consumed_at` 即时落库（示例 id: 19/20/21）。

说明：

- `pause/resume` 当前为“协作式软控制”，不是远端 codex 任务的硬暂停。
- 历史遗留会话里存在长期 `running` 个例，会影响写槽等待时长。

## 4. 未完成项（明日继续）

优先级 P1：

1. 会话治理：增加“僵尸 running 会话”清理/恢复机制（API 或脚本）。
2. `retry` 语义补全：明确不同 phase 下的真正重试策略，不仅是状态级响应。
3. 构建链路 smoke test：沉淀一键验收脚本（REST + WS/SSE + internal notify + control actions）。

优先级 P2：

1. `CODEX_PROVIDER` 抽象（`responses|mcp`），统一 Dashboard 执行入口。
2. 对接 `mcp-server-codex` 作为可切换 provider 并补配置项。

## 5. 明日续跑步骤

先读：

- `docs/progress/LATEST.md`
- `docs/progress/2026-03-29-progress.md`（本文件）

然后执行：

```bash
cd /data/claude_moreagent/src/dashboard
npm run dev:server
```

如需前端联调，再开一个终端：

```bash
cd /data/claude_moreagent/src/dashboard
npm run dev:client
```

基础健康检查：

```bash
curl -sS http://127.0.0.1:3100/api/health
curl -sS http://127.0.0.1:3100/api/projects
```

## 6. 续跑建议起手任务

建议明天第一步先做“历史 running 会话清理”，避免写槽被旧会话占用后影响新验收效率。

